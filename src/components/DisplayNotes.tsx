import { Button, Field, Form, TextArea, Card } from '@grafana/ui';
import React, { useEffect, useState } from 'react';
import { css } from 'emotion';

export const DisplayNotes = ({ x, y }: { x?: number; y?: number }) => {
  return <Card></Card>;
};
